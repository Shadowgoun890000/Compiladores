// Ejemplo simple de declaraciones y expresiones

let x = 10;
let y = 20;
const z = x + y * 2;

let nombre = "Juan";
let edad = 25;
let esEstudiante = true;

// Operaciones aritméticas
let suma = x + y;
let resta = x - y;
let mult = x * y;
let div = x / y;

// Expresiones complejas
let resultado = (x + y) * (z - 10) / 2;
