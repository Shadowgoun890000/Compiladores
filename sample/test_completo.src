// Test completo del analizador sintáctico

// Variables y constantes
let x = 10;
const PI = 3.14;
let nombre = "Ana";
let activo = true;

// Funciones
function suma(a, b) {
    return a + b;
}

function mensaje(texto) {
    let prefijo = "Mensaje: ";
    return prefijo;
}

// Condicionales
if (x > 5) {
    let resultado = x * 2;
} else {
    let resultado = x + 1;
}

// Bucles
let i = 0;
while (i < 10) {
    i = i + 1;
}

for (let j = 0; j < 5; j = j + 1) {
    let cuadrado = j * j;
}

// Expresiones complejas
let expr1 = (x + 5) * (x - 3);
let expr2 = x > 5 && x < 15;
let expr3 = !activo || x == 10;
